# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=operator_controller
DB_USER=operator
DB_PASSWORD=operator
DB_MIN_POOL_SIZE=5
DB_MAX_POOL_SIZE=20

# Controller Configuration
RECONCILE_INTERVAL=60
MAX_CONCURRENT_RECONCILES=5

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
LOG_LEVEL=INFO
CORS_ENABLED=false
# CORS_ORIGINS=http://localhost:3000,https://example.com

# Plugin Configuration
ENABLED_ACTION_PLUGINS=github_actions
ENABLED_INPUT_PLUGINS=http

# GitHub Actions Plugin Configuration
GITHUB_TOKEN=ghp_your_personal_access_token
GITHUB_API_URL=https://api.github.com
GITHUB_ACTIONS_TIMEOUT=3600
GITHUB_ACTIONS_POLL_INTERVAL=10

# Generic Plugin Configs (JSON format)
# PLUGIN_CONFIGS={"my_plugin": {"key": "value"}}
